import 'package:flutter/material.dart';

class DataBarangScreen extends StatelessWidget {
  const DataBarangScreen({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('Data Barang Screen'),
    );
  }
}